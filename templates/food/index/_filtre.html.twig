{{ form_start(form, {attr: {class : 'filter'}})}}

  <div>
    <h4 style="padding:0">Menu</h4>
    {{ form_row(form.q)}}
  </div>

  <div class="mt-5">
    <h4 style="padding:0">Prix</h4>
    <div class="row justify-content-around">
      <div class="">
        {{ form_row(form.min)}}
      </div>
      <div class="">
        {{ form_row(form.max)}}
      </div>
    </div>
  </div>

  <div class="mt-5">
    <button class="btn btn-danger" type="submit" style="width:100%">Filtrer</button>
  </div>
{{ form_end(form)}}
